# 01. 要件と決定事項

## 1.1 プロダクト定義

スマートフォンのブラウザで、手持ちの写真1枚から3D Gaussian Splatting を生成し、その場で回して見て、ファイルとして書き出せるWebアプリ。

一言で言えば **「写真を立体にして持ち帰る」** ツール。生成はすべて端末内で行われ、写真がネットワークに出ることはない。

## 1.2 確定した決定事項

ヒアリングで確定した内容。以降の全設計はこれを前提とする。

| # | 項目 | 決定 | 影響範囲 |
|---|---|---|---|
| D1 | 生成の実行場所 | **完全オンデバイス**（ブラウザ内100%） | 使用モデルの規模上限を決定づける。→ §1.3 |
| D2 | 出力の立体性 | **半球カバー**（±60°程度、裏側は補完） | 背面シェル生成が必須機能に。→ [03](./03-pipeline.md) |
| D3 | 対象被写体 | **人物ポートレート** および **物体・商品単体** | 背景除去が必須。風景シーンは非スコープ |
| D4 | 軽量化の最優先KPI | **スマホでの生成速度** | アルゴリズム選定の第一基準 |
| ~~D5~~ | 対応ブラウザ | ~~WebGPU必須~~ → **D19 で差し替え** | PoC-1 で前提が崩れた |
| D6 | ライセンス方針 | **未定・両方に備える**（差し替え可能な構造、既定は商用可） | モデルレジストリ機構が必要 |
| D7 | モデル配信 | **同一オリジン配信**（Pages成果物に同梱、gitにはコミットしない） | CORS不要。→ §1.5 |
| D8 | 保存・共有 | **ファイルダウンロードのみ** | サーバ不要。URL共有・動画書き出しは非スコープ |
| D9 | レンダラ | **Three.js ネイティブ WebGPU splat** (r186+) | 未リリースにつき緩衝レイヤ必須。→ [06](./06-rendering.md) |
| D10 | CIの責務 | ビルド+Pagesデプロイ / モデル量子化(キャッシュ付) / 自動テスト / 性能バジェット監視 の**4つすべて** | → [07](./07-cicd.md) |
| D11 | UI | 日本語のみ。~~PWAなし~~ → **Service Worker は入れる**（D20） | i18n は実装しない |
| D12 | 入力手段 | ファイル選択 / ドラッグ&ドロップ・クリップボード貼付 / サンプル画像同梱 | カメラプレビュー(getUserMedia)は非スコープ |
| D13 | 性能SLO | **10秒以内。その予算内で品質を最大化** | → §1.4 |
| D14 | 設計書形式 | `docs/` に Markdown + 閲覧用HTMLページ | 本文書群 |
| **D15** (v2) | 深度モデル | **Depth Anything 3 Small を第一候補、V2 Small を代替**。PoC-1/2 で実機比較して確定 | 形状精度・焦点距離推定。→ [03 §3.4](./03-pipeline.md#34-②-深度推定) |
| **D16** (v2) | 遮蔽部の補完 | **MI-GAN による軽量インペイントを追加**（MIT、uint8 約8MB） | 斜めから見た品質。→ [03 §3.7](./03-pipeline.md#37-⑧-遮蔽部のインペイント) |
| **D17** (v2) | 作業グリッド | **1024² を既定**（軽量プリセットのみ 512²） | ガウシアン数・出力サイズが約4倍。→ [04](./04-compression.md) |
| **D18** (v2) | 微調整 | **QAT を廃し、短い継ぎ目補正（40〜60反復・256²）に縮小** | 浮いた時間をインペイントと深度に再配分。→ [03 §3.8](./03-pipeline.md#38-⑨-継ぎ目補正) |
| **D19** (v3) | 対応ブラウザ | **WebGPU 優先＋WASM/WebGL2 フォールバック**（D5 を差し替え） | WebGPU があれば高速経路、無ければ互換経路。WebGL2 レンダラの新規実装が必要 |
| **D20** (v3) | マルチスレッド | **coi-serviceworker で `crossOriginIsolated` を成立させ、2 スレッドで動かす**（D11 の一部を差し替え） | PoC-2 実測で 1.74×。4 スレッドでも 1.87× 止まりで発熱の兆候が出たため 2 を既定に |
| **D21** (v3) | 性能SLO | **バックエンド別＋ユーザー選択**（D13 を差し替え） | WebGPU 10 秒 / WASM 軽量 **12 秒** / WASM 高品質 30 秒。→ §1.4 |
| **D22** (v3) | 量子化方式 | **バックエンド別**（WebGPU=q4f16 / WASM=uint8） | PoC-1 で WASM は q4f16 が最遅・uint8 が最速と判明 |
| **D23** (v3) | 深度モデル | **DA3-Small で確定**（D15 の PoC 待ちを解消） | 実機動作を確認。`intrinsics` から焦点距離が直接得られ、EXIF も画角仮定も不要に |

### D4 と D13 の関係について

「最優先KPIは生成速度」（D4）と「10秒以内で良いから高品質」（D13）は一見矛盾するため、次のように解釈を確定する。

> **10秒は超えてはならない上限（ハードバジェット）であり、その予算内では品質に投資する。**
> 速度が最優先KPIであるとは、「品質のために10秒を12秒に延ばす」という判断を禁じる、という意味である。

したがって設計上のルールは次の2つになる。

1. どの品質向上策も、時間予算表（[03](./03-pipeline.md#38-時間予算)）に枠を確保できる場合にのみ採用する。
2. 予算を超えそうな処理は、削るのではなく **プレビュー後のバックグラウンド処理に回す**。ユーザーが待つ時間と、処理が終わる時間を分離する。

## 1.3 なぜ完全オンデバイスが TripoSplat を排除するのか

D1（完全オンデバイス）は、使えるモデルの規模を厳しく制限する。TripoSplat の公式重みを実測した結果は次の通り。

| ファイル | サイズ (fp16) |
|---|---|
| `clip_vision/dino_v3_vit_h.safetensors` | 1,681 MB |
| `diffusion_models/triposplat_fp16.safetensors` | 741 MB |
| `vae/triposplat_vae_encoder_fp16.safetensors` | 682 MB |
| `vae/triposplat_vae_decoder_fp16.safetensors` | 576 MB |
| `background_removal/birefnet.safetensors` | 445 MB |
| `vae/flux2-vae.safetensors` | 336 MB |
| **合計** | **約 4,461 MB** |

int4 まで量子化しても1GBを超え、さらに20ステップの拡散サンプリングを要する。
iOS Safari はタブのメモリ使用量が概ね1〜1.5GBを超えると強制終了するため、モバイルブラウザでの実行は成立しない。
また GitHub Pages は静的ホスティングのみでサーバ推論を置けない（D7・D8）。

**結論**: オンデバイスで実現可能なのは、単眼深度推定を軸とした幾何ベースの再構成である。
これは 360° の完全なオブジェクトを作る手法ではないが、D2（半球カバー）の範囲であれば十分な品質に到達できる。

## 1.4 性能SLO

基準端末: **iPhone 14 / Google Pixel 7 相当**（2022年発売の中位機）。

| 指標 | 目標 | 上限（超えたらCI失敗またはリリース判定NG） |
|---|---|---|
| 指標 | 目標 | 上限 |
|---|---|---|
| **WebGPU 経路** — 画像選択 → 操作可能なプレビュー | 4.8 秒 | 6.0 秒 |
| **WebGPU 経路** — 全処理完了 | 7.1 秒 | **10.0 秒** |
| **WASM 経路・軽量プリセット** — 全処理完了 | 11 秒 | **12.0 秒** |
| **WASM 経路・高品質プリセット** — 全処理完了 | 22 秒 | **30.0 秒** |
| 初回訪問時のモデルダウンロード（人物モード） | 36.6 MB → **実測 38.5 MB (WebGPU) / 46.4 MB (WASM)** | 50 MB |
| 初回訪問時のモデルダウンロード（物体モード追加分） | 44 MB → **実測 44.35 MB** | 50 MB |
| ONNX Runtime の wasm（gzip、上記とは別に必要） | 6.5 MB | 9 MB |
| 閲覧時のフレームレート（WebGPU・標準 約42万ガウシアン） | 60 fps | 30 fps を下回らない |
| 閲覧時のフレームレート（WebGL2） | 30 fps | 20 fps を下回らない |
| `.pgs` 書き出しサイズ（標準プリセット） | 750 KB | 1.0 MB |
| JSバンドル (gzip) | 280 KB | 400 KB |

上記のうち**時間に関する指標はCIで測定できない**（GitHubランナーにGPUがなく、WebGPUはソフトウェアエミュレーションになるため）。
CIが強制するのはサイズと品質の静的指標のみで、時間は実機での手動計測を記録する。詳細は [07](./07-cicd.md#75-性能バジェットの現実的な線引き)。
実機の計測結果は [measurements.md](./measurements.md) に蓄積する。

**WASM 経路の数値は D20（マルチスレッド・2 スレッド）が有効なことを前提にしている。**
PoC-2 の実機実測では、人物・軽量プリセットで単スレッド 17.7 秒 → 2 スレッド 11.0 秒。
マルチスレッド無しでは 12 秒 SLO にも届かない。

## 1.5 D7「リポジトリ同梱」の正確な意味

「同梱」には2つの解釈があり、ヒアリングでの回答（同梱 + CIで量子化しキャッシュ）は次の組み合わせを指す。

- **git リポジトリには量子化済みバイナリをコミットしない。** モデル更新のたびに履歴が肥大化し、Pages の 1GB 制限を圧迫するため。
- **CI が量子化した成果物を、ビルド成果物として `dist/models/` に配置し Pages にデプロイする。**

結果として、ブラウザから見れば `https://<user>.github.io/<repo>/models/*.onnx` という**同一オリジン**でモデルが配信される。
CORS 設定が不要で、外部サービス（HuggingFace CDN 等）の可用性にも依存しない。リポジトリは軽いまま保たれる。

## 1.6 スコープ外（今回は作らないもの）

明示的に作らないものを列挙する。将来やるかもしれないが、本設計の対象外。

- 360°の完全なオブジェクト生成（TripoSplat 相当）。オンデバイスでは実現不可能。
- 複数枚の写真からの再構成（Structure from Motion / 一般的な3DGS学習）。
- サーバへのアップロード、URL共有、生成物のクラウド保存。
- 動画・GIFの書き出し。
- カメラのライブプレビュー（`getUserMedia`）。ファイル選択ダイアログからOS標準のカメラを呼べるため、優先度が低い。
- PWA・オフライン動作・Service Worker。
- 多言語対応。
- 風景・室内シーン全体の再構成（被写体が画面全体を占めるため背景除去が機能せず、必要ガウシアン数も跳ね上がる）。
- WebGL2 フォールバック（描画・推論とも）。ただし推論のみ WASM フォールバックを持つ。→ [08](./08-risks.md#r1)

## 1.7 参考文献の評価

ご提示いただいた2文献（TripoSplat＝単一画像からの生成、KISS-GS＝軽量化）について、調査した結果を記録する。役割はご提示のとおりである。

### KISS-GS — 本設計の主要な下敷き

[KISS-GS: 3D Gaussian Splatting Compression Kept Simple](https://fraunhoferhhi.github.io/KISS-GS/) (Fraunhofer HHI, ECCV 2026, arXiv 2608.26948)

単一画像からの生成手法ではなく、**学習済み3DGSシーンの圧縮パイプライン**である。次の3段を独立に適用する。

1. **Compaction**: プルーニング手法の組み合わせでプリミティブ数を 15.7× 削減
2. **Encoding (SOG-XT)**: 属性を2D画像グリッドに並べ替え、8bit画像コーデックで圧縮して 6.6× 削減。自己組織化2Dコードブックと PRAS (Parallel Representative Assignment Smoothing) を導入
3. **符号化を織り込んだ微調整**: 圧縮をループに含めた最適化で追加 2.2× 削減

総合で vanilla 3DGS 比 85〜319×。Garden シーンの 256k スプラットが 3.94MB。デコードは NumPy 実装で 108ms、GPU不要。WebGL でのブラウザ再生実績あり。

**本設計への採り入れ方**: 4階層の軽量化のうち、L2（属性の2D画像化）と L3（量子化を織り込んだ微調整）は KISS-GS の思想をそのまま踏襲する。
ただし後述の通り、**単一画像ケースでは最も重い工程である「並べ替え」が不要になる**という構造的な優位がある。→ [04](./04-compression.md#43-l2-属性の2d画像化)

### TripoSplat — 到達点の参照のみ

[TripoSplat](https://github.com/VAST-AI-Research/TripoSplat) (VAST-AI Research, MIT ライセンス、重みも MIT)

単一の2D画像から高品質な3DGSを生成する。DINOv3 ViT-H による画像エンコード → triplane 生成 → ガウシアンデコード。最大 262,144 ガウシアン、可変個数に対応。出力は `.ply` / `.splat`。

**本設計への採り入れ方**: §1.3 の通り規模が大きすぎるため、モデルとしては採用しない。
ただし「ガウシアン数を可変にして品質と描画コストをトレードオフさせる」という設計思想と、上限 262,144 という数値は、本設計のガウシアン予算（[04](./04-compression.md#42-l1-個数の削減)）の参考にしている。

## 1.8 用語

| 用語 | 意味 |
|---|---|
| 3DGS | 3D Gaussian Splatting。シーンを多数の3次元ガウシアン（楕円体）の集合で表現し、それを画面に投影(splat)して描画する手法 |
| ガウシアン / スプラット | 3DGSの構成単位。位置・スケール・回転・不透明度・色を持つ |
| SH | Spherical Harmonics（球面調和関数）。3DGSで視点依存の色を表現するのに使われる。次数0はDC成分＝視点非依存の単色 |
| サーフェル (surfel) | 厚みを持たない平たいガウシアン。法線方向のスケールがほぼ0のもの |
| 前面シェル | 入力画像から直接見えている面を表すガウシアン群 |
| 背面シェル | 見えていない裏側を推定して生成するガウシアン群。半球カバー(D2)の実現に必須 |
| スカート (skirt) | 深度の不連続な縁に沿って生成する、視点移動時に穴が開くのを防ぐガウシアン群 |
| 遮蔽解消 (disocclusion) | 視点を動かした結果、それまで隠れていた領域が露出すること |
| QAT | Quantization-Aware Training。量子化誤差を織り込んだ最適化 |
| STE | Straight-Through Estimator。量子化のような微分不可能な演算を勾配計算で素通しさせる手法 |
| PLAS | Parallel Linear Assignment Sorting。3DGS属性を2Dグリッドに空間的コヒーレンスを保って並べ替えるアルゴリズム |
| SLO | Service Level Objective。守るべき性能目標 |

## 1.9 改訂履歴（v1 → v2）

v2 は「スマホでより品質の高い3DGS」を目標に、v1 を第三者レビューして見直したもの。変更は品質に直結するものに絞った。

| # | v1 の問題 | v2 での変更 | 参照 |
|---|---|---|---|
| 1 | 微調整の見積もり「1反復 6ms × 400 = 2.4s」が非現実的。微分可能ラスタライズの forward+backward はデスクトップGPUでも 10〜20ms/反復で、スマホでは 16〜32 秒になる | **QAT を廃止。** 40〜60反復・256² の「継ぎ目補正」に縮小し、浮いた約2秒をインペイントと深度に再配分 | D18, [03 §3.8](./03-pipeline.md#38-⑨-継ぎ目補正) |
| 2 | 微調整の教師が入力視点1枚のため、効果が入力視点にしか及ばず、斜めから見た品質に寄与しない | 同上。品質投資の主軸を「入力視点の再現」から「見えない部分の補完」へ移した | [03 §3.7](./03-pipeline.md#37-⑧-遮蔽部のインペイント) |
| 3 | Depth Anything V2 の出力は affine-invariant な**逆深度**で、未知のシフト項 b が被写体の膨らみ/平坦さを直接歪める。v1 の較正は尺度 a しか実質的に決めていなかった | **Depth Anything 3 Small を第一候補に**（深度を直接予測、焦点距離も推定）。V2 経路にはシルエット法線の事前分布による b の推定を追加 | D15, [03 §3.5](./03-pipeline.md#35-③-深度較正) |
| 4 | 遮蔽解消が「奥側の色の引き伸ばし」で、斜めから見たときの破綻が最も目立つ | **MI-GAN（MIT、モバイル向け）で遮蔽領域をインペイント**。スカートの色を補完テクスチャからサンプルする | D16, [03 §3.7](./03-pipeline.md#37-⑧-遮蔽部のインペイント) |
| 5 | 人物の背面色が「顔の水平反転」で不気味（v1 自身も R3 で懸念） | 人物モードでは鏡像を使わず、**シルエット縁の色を行方向に伸ばした無地陰影**に。鏡像は物体モードのみ | [03 §3.6.2](./03-pipeline.md#362-⑤-背面シェル) |
| 6 | 作業グリッド 512² は写真の解像度に対し低く、色プレーン＝写真そのものの品質天井になっていた | **1024² を既定に。** 深度タイルパス（1036²）と解像度が揃う | D17 |
| 7 | 深度エッジの「飛び画素」と、αの軟化境界に混ざった深度への対策が無かった | **エッジ保存の深度シャープ化**（色とαをガイドにしたジョイントバイラテラル）と、境界画素の深度を内側から引く処理を追加 | [03 §3.5.3](./03-pipeline.md#353-エッジ保存シャープ化と境界処理) |
| 8 | 適応サンプリングで統合したガウシアンを `.pgs` にどう格納するか未定義 | 読込時の**決定的な再計算**として仕様化。継ぎ目補正の結果は画素プレーンに書き戻す | [05 §5.2.3](./05-data-formats.md#523-読み込み手順) |
| 9 | 拡大縮小時のエイリアシング対策が無かった | Mip-Splatting 流の 2D 低域フィルタと 3D スケール下限を描画に追加 | [06 §6.10](./06-rendering.md#610-アンチエイリアス) |
| 10 | 快適に見られる視点範囲を ±60° としていたが、2.5D 表現では楽観的 | 快適範囲 ±45°/±25°、ハード上限 ±60°/±35° に分け、ビネットは 35° から | [06 §6.7](./06-rendering.md#67-カメラ操作) |
| 11 | 「参考文献の役割が逆」という記述は、原指示では正しく書かれていた | 記述を訂正 | §1.7 |

### 実装フェーズで判明した訂正（v2.1 / 2026-09-07）

設計を実装に移す過程で、設計書の記述が実測と食い違った点。いずれも結論は変わらない。

| # | 箇所 | 訂正 |
|---|---|---|
| 12 | [04 §4.4](./04-compression.md#44-l2--数値精度の削減) スケールの精度 | 「8bit で 0.55%」はレンジ比 16 のときの値。実際に出うるレンジ比は約 51 で **0.77%**。サーフェル半径2pxに対し 0.016px なので結論（8bit で足りる）は不変。実装では書き出しごとにレンジを締める |
| 13 | [04 §4.4](./04-compression.md#44-l2--数値精度の削減) 不透明度の根拠 | 「素の α を量子化すると 0 付近と 1 付近が粗くなる」は不正確。粗くなるのは**透過率 (1−α)** で、α→1 で線形量子化は 17〜31% の相対誤差を出す。logit はそこを 0.6% に保つ |
| 14 | [07 §7.4](./07-cicd.md#e2e-での-webgpu) CI の WebGPU | 開発コンテナの headless Chromium では SwiftShader ICD を指定しても `navigator.gpu` が露出しなかった。E2E を能力検出で分岐させ、未検証であることを結果に明記する方式に変更 |
| 16 | [03 §3.5.2](./03-pipeline.md#352-逆深度の係数を閉形式で解くv21-で全面的に差し替え) シフト推定 | **設計の方法が成立しない。** シルエット帯の `Σ(n·v)²` は b について単調で内点最小値を持たず、最小化すると際限なく膨らむ。さらに測ると、奥行き幅を固定すれば b は形を 5% 以内しか変えない。最適化をやめ、「中央値を 1.0」「幅を目標値」の2制約から閉形式で解く方式に差し替えた（合成球で誤差 0.000%）。約 60ms の削減にもなった |
| 15 | [07 §7.5](./07-cicd.md#75-性能バジェットの現実的な線引き) バジェット | **ONNX Runtime Web の wasm 自体が 27.8 MB（gzip 6.5 MB）** で、v2 の見積もりに入っていなかった。初回訪問の実コストはモデル 36.6 MB に加えてこれがかかる。バジェットに `runtime.ort.wasm.gzip` を追加した |

### PoC-1 の結果を受けた改訂（v3 / 2026-09-07）

実機（Android 10 / Chrome 152）で PoC-1 を実行した結果、**設計の前提が2つ覆った**。
実測値は [measurements.md](./measurements.md)。

| # | 判明したこと | 設計の変更 |
|---|---|---|
| 17 | **WebGPU が使えない実機が現実に存在する。** Chrome の Android WebGPU は Android 12 以上が要件で、対象端末は非対応だった | D5「WebGPU 必須」を **D19「WebGPU 優先＋WASM/WebGL2 フォールバック」**に差し替え。WebGL2 レンダラを新規実装する |
| 18 | **WASM 単スレッドでは SLO の 3〜6 倍かかる**（人物 30 秒、物体 63 秒） | D21 でバックエンド別 SLO に。さらに D20 でマルチスレッド化（約 3 倍）を導入 |
| 19 | **WASM では q4f16 が最遅（19.8s）、uint8 が最速（12.9s）。** `MatMulNBits` に WASM の速い経路が無いため | D22「量子化方式はバックエンド別」。CI は両方を生成し、ブラウザが実行プロバイダに応じて選ぶ |
| 20 | **DA3-Small は動き、しかも `intrinsics` と `confidence` を出力する** | D23 で採用確定。**焦点距離が直接得られるので、EXIF も画角55°の仮定も不要になった**。`confidence` は設計に無かった収穫 |
| 21 | **ISNet の 1024² は WASM では 37.8 秒**で使えない | WASM 経路では 512² に落とす（`inputSizeWasm`） |
| 22 | DA3-S は fp32 のまま DA2-S の fp16 より速い（13.3s < 19.6s） | 第一候補としての妥当性が裏付けられた |

### PoC-2 の結果（2026-09-07）

| # | 判明したこと | 設計の変更 |
|---|---|---|
| 23 | **coi-serviceworker は実機で成功した。** `crossOriginIsolated: true` になり、WASM のマルチスレッドが使えた | D20 の手法自体は妥当と確認 |
| 24 | **ただしスケーリングは 2 スレッドで頭打ち**（1 → 1.74× → 1.87×）。論理コアは 8 あるが大コアが 2 つの big.LITTLE 構成と見られる。開発コンテナ（4 コア均質）の 3〜4× は携帯機の予測に使えなかった | D20 の既定スレッド数を **2** に。4 にしても 7% しか増えない |
| 25 | **4 スレッドで初回推論が定常推論より速いという逆転**が起き、発熱による性能低下を示唆していた | 連続生成の安定性を優先して 2 スレッドを選ぶ根拠になった |
| 26 | マルチスレッド化しても人物・軽量で 11.0 秒。**10 秒 SLO には届かない** | D21 の WASM 軽量 SLO を **12 秒**に緩めた |

PoC ハーネスの自動判定が「2× 未満なら効果が小さい」という**要件から導いていない恣意的な閾値**で
誤解を招く結論を出していたので、SLO 基準の判定に書き直した。

D11「PWA なし」のうち Service Worker を入れない方針は **D20 で撤回**した。理由は
「WebGPU があれば WASM マルチスレッドは不要」だったが、その前提が崩れたため。
i18n を入れない方針は変えていない。

v1 から**変えていない**主要判断: 完全オンデバイス（D1）、半球カバー（D2）、WebGPU 必須（D5）、同一オリジン配信（D7）、
表現の削減（SH次数0・サーフェル化）、属性の画像化と「並べ替え不要」の論理、GitHub Pages / CI の構成。
